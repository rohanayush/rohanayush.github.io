# build output
dist/
# dependencies
node_modules/
# astro
.astro/
# release artifacts (large binaries — host them on GitHub Releases instead)
public/downloads/*.exe
public/downloads/*.msi
public/downloads/*.apk
# staging folder for built installers to upload to the GitHub release tag
release/
# env
.env
.env.production
# working notes, not part of the site
process.md
# local tool config (holds machine-specific absolute paths)
.claude/
